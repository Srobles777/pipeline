pipeline {
  
    agent any
    
    stages    {
    
        stage("build") {
          
            steps {
                echo 'buid steps 1 test aplications $branch (main)'
                       }
                  }
        stage("test") {
            
            steps {
                echo 'test steps $branch (main)'
                      }
                  }
        stage("deploy1") {
          
            steps {
                echo 'deploy steps $branch (main)'
                          }
                  }
              }
        } 
